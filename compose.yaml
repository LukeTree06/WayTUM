services:
  app:
    build: .
    container_name: waytum-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./public:/usr/src/app/public